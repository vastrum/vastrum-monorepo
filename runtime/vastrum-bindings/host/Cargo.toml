[package]
name = "vastrum-bindings-host"
version = "0.1.0"
edition = "2024"

[dependencies]
wasmtime = { version = "42", default-features = false, features = ["cranelift", "runtime", "std", "async", "threads", "parallel-compilation", "pooling-allocator"] }
vastrum-shared-types.workspace = true

